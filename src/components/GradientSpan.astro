---
export interface Props {
	gradient?: string;
	class?: string;
	id?: string;
}

const { gradient = 'from-primary to-secondary', class: className, id } = Astro.props;
---

<span {id} class:list={[gradient, 'bg-linear-to-r bg-clip-text text-transparent', className]}>
	<slot />
</span>
